# AI Experiments
A collection of experiments with AI. It was fun.

Includes both experiments with existing AI models (like ChatGPT) and experiments with AI concepts (like creating a perceptron and multi layer neural network).
